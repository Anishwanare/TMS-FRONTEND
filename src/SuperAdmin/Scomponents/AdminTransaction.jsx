import React from 'react'
import { useSelector } from 'react-redux';

const AdminTransaction = ({ id }) => {
    // what i am thinking i will make a transaction model in backend and on the basic of id i will find a transaction through id

    return (
        <div>
            Transaction component
        </div>
    )
}

export default AdminTransaction
